# M6 Slice 1 Payloads

`v1_happy.json` is the pinned params object for the thinnest phase 0 smoke
run.

Provenance: derived from `no_instruction_v0_tx_b64`; bincode-verified;
execute-verified live on 2026-06-11/12; dummy blockhash is acceptable because
the procedure uses `replace_recent_blockhash=true`.
